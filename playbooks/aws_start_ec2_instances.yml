---
- name: Start EC2 Instances
  hosts: localhost
  connection: local
  gather_facts: False
  vars:
    change_state: running
    current_instance_state: stopped
  vars_files:
    - ../vars/credentials.yml
  roles:
    - gather_facts
    - ec2_instances_change_state