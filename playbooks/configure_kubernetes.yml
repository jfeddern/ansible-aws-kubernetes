---
- name: Install and Setup Kubernetes on AWS
  hosts: localhost
  connection: local
  gather_facts: False
  vars_files:
    - ../vars/credentials.yml
  roles:
    - kubernetes_user
    - kubernetes_dashboard
    - kubernetes_monitoring
    - kubernetes_external_dns
    - kubernetes_credentials